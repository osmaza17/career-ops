# Customization Guide

## Profile (config/profile.md)

This is the single source of truth for career-ops. It has two layers: a YAML frontmatter block (identity, targets, narrative, compensation, languages) and a markdown body (your full trajectory corpus). All modes read from here.

Key sections:
- **candidate**: Name, email, phone, location, LinkedIn, portfolio
- **target_roles**: Your North Star roles and archetypes
- **narrative**: Your headline, exit story, superpowers, proof points
- **compensation**: Target range, minimum, currency
- **location**: Country, timezone, visa status, on-site availability

## Target Roles & Strategy (config/profile.md)

The archetype table in `config/profile.md` (under `target_roles.archetypes` and `strategy.adaptive_framing`) determines how offers are scored and CVs are framed. Edit these keys to match YOUR career targets:

```yaml
target_roles:
  archetypes:
    - name: Your Role 1
      thematic_axes: key skills
      value_proposition: what they need

strategy:
  adaptive_framing:
    - archetype: Your Role 1
      proof_points: [project A, project B]
```

Also update `strategy.cross_cutting_advantage` to map YOUR specific projects across archetypes.

## Portals (config/portals.yml)

Create or edit `config/portals.yml` (generated by `/career-ops scan` or manual setup):

1. **title_filter.positive**: Keywords matching your target roles
2. **title_filter.negative**: Tech stacks or domains to exclude
3. **search_queries**: WebSearch queries for job boards (Ashby, Greenhouse, Lever)
4. **tracked_companies**: Companies to check directly

## CV Template (config/profile.md)

The LaTeX template is embedded in `modes/pdf.md` under the **LaTeX Flow** section and compiled via `generate-pdf.mjs` (requires `tectonic` or `pdflatex` installed). The template itself is a system file — do not edit it directly. To customize your CV output, edit the content sections in `config/profile.md` (education, experience, projects, skills, etc.) — the template reads from there.

## Negotiation Scripts (config/profile.md)

Negotiation frameworks live in `config/profile.md` under the `strategy:` key — never in system files. Add your own:

```yaml
strategy:
  negotiation_scripts:
    initial_ask: "Based on my research and the scope of this role..."
    pushback: "I understand the band, but given [specific proof point]..."
    geographic_arbitrage: "My target range accounts for Paris cost of living..."
```

## Hooks (Optional)

Career-ops can integrate with external systems via Claude Code hooks. Example hooks:

```json
{
  "hooks": {
    "SessionStart": [{
      "hooks": [{
        "type": "command",
        "command": "echo 'Career-ops session started'"
      }]
    }]
  }
}
```

Save hooks in `.claude/settings.json`.

## States (states.yml)

The canonical states are defined in `states.yml` (a system file — do not edit directly). They rarely change. If you genuinely need a new status, open an issue on the project repo. `normalize-statuses.mjs` maps common aliases to canonical values automatically.
